# Pin these exactly — gfootball is sensitive to gym version
# gfootball is installed LAST (separately) — it must come after all other deps
# torch is installed separately in Dockerfile (own layer, slow download)
gym==0.21.0
stable-baselines3==1.8.0
numpy==1.23.5
statsbombpy==1.1.0
scipy==1.10.1
matplotlib==3.7.1
seaborn==0.12.2
pyyaml==6.0
tensorboard==2.12.0
scikit-learn==1.2.2
